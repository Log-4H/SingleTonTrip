<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AdMapper">
<!-- 광고 신청 폼에 리스트 뿌리기 -->
<select id="selectAdPrice"
		resultType="com.log4h.singletontrip.ad.domain.AdPriceVo">
	SELECT
		ad_price_cd,
		ad_price_detail,
		ad_price_perday,
		ad_price_amount,
		ad_price_total
	FROM
		tb_ad_price
</select>
<!-- 광고 신청 목록 -->
<select id="selectAdApplyList" parameterType="com.log4h.singletontrip.member.domain.LoginVo"
		resultType="com.log4h.singletontrip.ad.domain.AdVo">
	SELECT
		ad_no,
		company_id,
		ad_content,
		ad_img,
		ad_apply_date,
		approve_state_cd,
		payment_finish_date
	FROM
		tb_ad
	JOIN
		tb_payment
	ON
		payment_target_no = ad_no
	WHERE
		approve_state_cd = 1
	<if test="memberLevel == 2">
		WHERE member_id = #{memberId}
	</if>
</select>
<!-- 광고 신청 목록에서 승인 -->
<update id="adApprove" parameterType="map">
	UPDATE
		tb_ad
	SET
		approve_state_cd = 2,
		ad_reg_date = now(),
		ad_end_date = ADDDATE(now(), INTERVAL ad_total_amount DAY),
		admin_id = #{adminId}
	WHERE
		ad_no = #{adNo}
</update>
<!-- 광고 신청 목록에서 거절 -->
<update id="adRefuse" parameterType="map">
	UPDATE
		tb_ad a 
	JOIN
		tb_payment b
	ON
		a.ad_no = b.payment_target_no
	SET
		a.approve_state_cd = 3,
		a.admin_id = #{adminId},
		b.payment_state_cd = 3
	WHERE
		a.ad_no = #{adNo}
</update>
<!-- 광고 리스트 가져오기 -->
<select id="selectAdList"
		resultType="com.log4h.singletontrip.ad.domain.AdVo">
	SELECT
		ad_no,
		company_id,
		ad_content,
		ad_img,
		ad_reg_date,
		ad_end_date,
		admin_id
	FROM
		tb_ad
	WHERE
		approve_state_cd=2
</select>
<!-- 광고 이미지 파일 검색 -->
<select id="selectAdImg" parameterType="com.log4h.singletontrip.ad.domain.AdVo"
		resultType="String">
	SELECT
		ad_img
	FROM
		tb_ad
	WHERE
		ad_no = #{adNo}
</select>
<!-- 광고 리스트에서 이미지파일 업로드 -->
<update id="adImgUpload" parameterType="com.log4h.singletontrip.ad.domain.AdVo">
	UPDATE
		tb_ad
	SET
		ad_img = #{adImg}
	WHERE 
		ad_no = #{adNo}
</update>
<!-- 광고 신청 등록 -->
<insert id="adApply" parameterType="com.log4h.singletontrip.ad.domain.AdVo" useGeneratedKeys="true"  keyProperty="adNo" >
	INSERT INTO tb_ad 
		(
			company_id,
			ad_price_cd, 
			ad_content, 
			ad_total_amount,
			ad_total_price, 
			ad_apply_date
		) 
	SELECT
			#{companyId},
			ad_price_cd, 
			ad_price_detail, 
			ad_price_amount, 
			ad_price_total, 
			now() 
	FROM 
			tb_ad_price 
	WHERE 
			ad_price_cd = #{adPriceCd}
</insert>
<!-- 광고 신청 등록 -->
<insert id="adApplyPayment" parameterType="com.log4h.singletontrip.ad.domain.AdVo">
	INSERT INTO tb_payment
		(
			member_id,
			payment_cate_cd,
			payment_target_no,
			payment_total_price, 
			payment_apply_date
		) 
	SELECT
			company_id,
			2, 
			#{adNo}, 
			ad_total_price, 
			ad_apply_date 
	FROM 
			tb_ad
	WHERE 
			ad_no = #{adNo}
</insert>
<!-- 결제 목록 가져오기 -->
<select id="paymentList"
		resultType="com.log4h.singletontrip.ad.domain.AdVo">
	SELECT
		ad_no,
		ad_content,
		ad_total_price
	FROM
		tb_ad
	JOIN
		tb_payment
	ON
		member_id = company_id
	WHERE
		member_id = #{companyId}
	AND
		payment_state_cd=1
	AND
		approve_state_cd=1
	GROUP BY
		ad_no
</select>
<!-- 결제 목록들의 합계 구하기 -->
<select id="totalPrice"
		resultType="int" >
	SELECT 
		COALESCE(SUM(payment_total_price),0) 
	FROM 
		tb_payment
	WHERE 
		member_id = #{companyId}
	AND
		payment_state_cd=1
</select>
<!-- 광고 신청 목록에서 삭제 -->
<delete id="deleteAdApplyList" parameterType="com.log4h.singletontrip.ad.domain.AdVo">
	DELETE 
	FROM
		tb_ad
	WHERE
		ad_no = #{adNo}
</delete>
<!-- 광고 신청 목록에서 삭제 후 결제 등록 목록에서도 삭제 -->
<delete id="deletePayAdList" parameterType="com.log4h.singletontrip.ad.domain.AdVo">
	DELETE
	FROM
		tb_payment
	WHERE
		payment_target_no = #{adNo}
</delete>
<!-- 결제 -->
<update id="paymentAd" parameterType="map">
	UPDATE
		tb_payment
	SET
		payment_state_cd=2,
		payment_price=#{total},
		payment_finish_date=now()
	WHERE
		member_id=#{companyId}
</update>
</mapper>